let firstInput = prompt("First number");
let method = prompt("add or substract?");
let secondInput = prompt("Second number");
let result;
let combinedInput = firstInput + secondInput;

alert(firstInput + method + secondInput);

alert(combinedInput);

if (combinedInput === isNaN) {
    alert("not numbers");
} 
else if (method.toLowerCase === "add" || "+") {
    result = +firstInput + +secondInput;
    alert(result);
}
else if (method.toLowerCase === "substract" || "-") {
    result = +firstInput + +secondInput;
    alert(result);
}

// works but some holes 